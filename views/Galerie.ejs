<% include partials/head %>

<div class="full-container full-container-one">
	<div class="container">
		<h1 class="square-title text-center mb-3">Art Gallery</h1>

		<h2 class="secondary-title text-center">Replicas of these paintings are available for sale by clicking on the "<b>See more</b>" button</h2>
		<div class="optional-title-wrapper">
			<h3 class="optional-title text-center">Not interested in replicas?</h3>
			<input type="submit" class="submit-btn call-btn-small" value="Buy the original version!" id="goto-shop" />
		</div>
		<div class="container-gallery mt-3" id="container-gallery">
			<% if (locals.galleries){ %> <% Object.keys(locals.galleries).forEach(function (index) { %>
			<div class="expandable-card" id="<%= locals.galleries[index]._id %>">
				<div class="face face1 blog-overlay-wrapper mt-0">
					<a href="#expand">
						<img src="/api/image/<%= locals.galleries[index].mainImgId %>" class="w-100 expandable" alt="Gallery Image - <%= locals.galleries[index].title %>" />
					</a>

					<div class="blog-overlay expandable" data-id="<%= locals.galleries[index]._id %>" data-isGallery="true">
						<h4>
							<i><a href="/Galerie/<%= locals.galleries[index]._id %>"><%= locals.galleries[index].shorttitle %></a></i>
						</h4>
						<div class="gallery-tags mt-2">
							<% locals.galleries[index].tags.forEach(function (tag) { %>
							<a href="/Galerie/Tags?t=<%= tag %>">#<%= tag %></a>
							<% }) %>
						</div>
						<form action="/Galerie/<%= locals.galleries[index]._id %>"><button class="blog-btn">See More</button></form>
					</div>
				</div>
			</div>
			<% }) %> <% } else { %>
			<div class="text-center bwhite">
				<h1>Gallery page is under maintenance, please come back later</h1>
			</div>
			<% } %>
		</div>
		<div class="col-md-6 offset-md-3 mt-3">
			<img alt="loader gif animation" id="loader" src="/img/loader.gif" />
			<input id="infinitebtn" type="submit" class="submit-btn" value="Load more" />
		</div>
	</div>
	<% include partials/footer %>
</div>

<a class="lightbox-close" href="#close">
	<div class="lightbox-target" id="expand">
		<img alt="Expanded Image" id="expandImg" src="" />
	</div>
</a>

<script src="/scripts/gallery/infiniteGalleries.js" defer></script>
